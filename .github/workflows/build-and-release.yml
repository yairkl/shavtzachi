name: Build and Release

on:
  push:
    branches:
      - master
      - 'release/*'

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-22.04, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Create credentials and config files
        shell: bash
        run: |
          echo '${{ secrets.CREDENTIALS_JSON }}' > credentials.json
          echo '${{ secrets.CONFIG_JSON }}' > config.json

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest-cov

      - name: Run Tests
        run: |
          python -m pytest --cov=. --cov-report=xml --cov-report=term-missing

      - name: Code Coverage Summary Report
        if: matrix.os == 'ubuntu-22.04'
        uses: irongut/CodeCoverageSummary@v1.3.0
        with:
          filename: coverage.xml
          badge: true
          format: markdown
          output: both

      - name: Add Coverage Summary to Job Summary
        if: matrix.os == 'ubuntu-22.04'
        run: cat code-coverage-results.md >> $GITHUB_STEP_SUMMARY

      - name: Run Build Script
        run: python build.py

      - name: Rename Executables
        shell: python
        run: |
          import os, sys
          src = "dist/shavtzachi.exe" if sys.platform == "win32" else "dist/shavtzachi"
          
          if sys.platform == "win32":
              dst = "dist/shavtzachi-windows.exe"
          elif sys.platform == "darwin":
              dst = "dist/shavtzachi-macos"
          else:
              dst = "dist/shavtzachi-linux"
              
          os.rename(src, dst)

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: executable-${{ matrix.os }}
          path: dist/*
          retention-days: 1

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist-tmp
          merge-multiple: true

      - name: Make Linux and MacOS binaries executable
        shell: bash
        run: |
          if [ -f dist-tmp/shavtzachi-linux ]; then chmod +x dist-tmp/shavtzachi-linux; fi
          if [ -f dist-tmp/shavtzachi-macos ]; then chmod +x dist-tmp/shavtzachi-macos; fi

      - name: Create or Update Latest Release
        uses: ncipollo/release-action@v1
        with:
          tag: "latest"
          name: "Latest Release"
          artifacts: "dist-tmp/*"
          allowUpdates: true
          makeLatest: true
          token: ${{ secrets.GITHUB_TOKEN }}
          body: "Automated rolling build of the latest version of Shavtzachi."
